# SPEC.md — command-type

<!-- scaffold template v1.6.0 から展開(2026-08-05)。以後このファイルはプロジェクトが育てる -->

Linux コマンドのタイピング練習ゲーム。本書が仕様の正本であり、実装と食い違う場合は本書が正。
変更は **スペック → テスト → 実装** の順に行う(スペック駆動)。

## 1. 目的

Linux の主要コマンド 150 個を「落ちものゲーム × タイピング」の形式で楽しく反復学習する
Web アプリを提供する。コマンド名を打鍵しながら、短い解説文とカテゴリ名を同時に目にすることで、
タイピング練習とコマンド知識の定着を一度に実現する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 出題データは 150 コマンド。各コマンドは `cmd`(コマンド名)/ `desc`(短い日本語解説)/ `category`(§4 の 13 分類)/ `level`(§4 の 3 レベル)を持つ。正本は `data/commands.json` | must |
| F-02 | レベル選択画面で初級・中級・上級を選ぶ。各レベルは §4 で対応づけたカテゴリ群からのみ出題する(混合はしない。混合モードは P3 の拡張) | must |
| F-03 | 現在のコマンドは、コマンド文字列・解説文・カテゴリ名の 3 点セットで画面上部から落下する。画面に同時に存在する出題は 1 つ | must |
| F-04 | タイプ入力はリアルタイムに照合し、一致した先頭部分と最初の不一致位置を視覚的に区別して表示する。Backspace で修正できる | must |
| F-05 | Enter で確定する。入力が完全一致なら「クリア」(得点加算・コンボ継続)して次のコマンドを出題する。不一致のまま Enter はミス 1(コンボリセット・入力は保持) | must |
| F-06 | コマンドが画面底部に到達したらミス 1(ライフ 1 消費)とし、次のコマンドを出題する | must |
| F-07 | 1 セッション = 20 問。ライフは 3 で、落下到達ミスが 3 回になった時点でセッション終了 | must |
| F-08 | スコア = クリアごとの基礎点 + 残り高度ボーナス(速く打つほど高い)+ コンボボーナス。算式は core の純関数として実装しテストで固定する | must |
| F-09 | 落下速度はレベル別の基準値から始まり、セッション内の進行(クリア数)に応じて漸増する | must |
| F-10 | 結果画面にスコア・正確率・CPM(打鍵/分)・出題コマンド一覧(解説付き、ミスしたものを強調)を表示する。一覧は復習を目的とする | must |
| F-11 | 出題順はセッション内重複なしのランダム。乱数はシード注入可能とし、テストで再現できる | must |
| F-12 | ハイスコアをレベル別に localStorage へ保存し、レベル選択画面に表示する | should |
| F-13 | 効果音と演出: クリア(上昇音 + 緑フラッシュ)・不一致 Enter(ブザー)・落下(下降音 + 赤フラッシュ)。効果音は Web Audio の合成音のみ(外部アセットなし)で、🔊 トグルで ON/OFF でき、設定は localStorage に保存(既定 ON)。演出は CSS アニメーションのみで実装する | could(P3) |
| F-14 | ミックスモード: レベル選択に「ミックス」を追加し、全 13 カテゴリ・150 コマンドから出題する。落下基準速度は中級と同値。ハイスコアは 3 レベルと独立に保存する | could(P3) |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Next.js 静的エクスポート(`output: 'export'`)で動作し、サーバ API を持たない。GitHub 連携の Vercel で公開する | `next build` 成功 + out/ のみで動作 |
| N-02 | 落下・判定ロジックは UI から分離した決定的な純関数(`src/core/`)とし、`requestAnimationFrame` の経過時間を入力に取る。描画は 60fps を目標 | core の単体テスト + 手動確認 |
| N-03 | ゲーム操作はキーボードのみで完結する。コマンド入力欄は IME を無効化し、半角英数のみ受け付ける | テスト + 手動確認 |
| N-04 | デスクトップブラウザ(Chrome / Edge / Firefox 最新)を対象。モバイルは表示崩れがないことのみ保証 | 手動確認 |

## 4. レベル・カテゴリ・出題数の対応(データ正本の配分)

150 コマンドは以下の 13 カテゴリに分かれ、カテゴリごとにレベルが一意に決まる。
この表は `data/commands.json` とテスト(T-001〜T-006)の正であり、変更は本書 → テスト → データの順で行う。

| level | category id | カテゴリ名 | 出題数 |
|---|---|---|---|
| 初級 | `nav-basic` | ナビゲーション・基本ファイル操作 | 18 |
| 初級 | `text-edit` | テキスト処理・ファイル編集 | 17 |
| 初級 | `perm-user` | 権限・ユーザー管理 | 15 |
| 中級 | `sysinfo-proc` | システム情報・プロセス管理 | 14 |
| 中級 | `archive-transfer` | 圧縮・展開・転送 | 12 |
| 中級 | `network-comm` | ネットワーク・通信 | 12 |
| 中級 | `pkg-env` | パッケージ管理・環境設定・その他 | 12 |
| 上級 | `adv-file-disk` | 高度なファイル・ディスク操作 | 9 |
| 上級 | `perf-analysis` | リソース・パフォーマンス分析 | 9 |
| 上級 | `adv-net-diag` | 高度なネットワーク診断 | 8 |
| 上級 | `term-bg` | ターミナル管理・バックグラウンド実行 | 8 |
| 上級 | `security-crypto` | セキュリティ・暗号化 | 8 |
| 上級 | `dev-debug` | 開発・デバッグ | 8 |

集計: 初級 50 / 中級 50 / 上級 50 = 150。

## 5. 品質基準

数値基準(すべてテストにトレースする):

- `data/commands.json`: 件数 150 / レベル配分 50・50・50 / カテゴリ配分が §4 の表と完全一致 /
  `cmd` の重複なし / `cmd` は `[a-z0-9+._-]+` のみ / `desc` は 8〜40 文字の日本語解説
- `src/core/` のカバレッジ: lines・functions・statements ≥ 90%、branches ≥ 85%(bungo-type と同水準)
- 完了条件: `npm run verify`(typecheck + lint + test + build)green + `python harness/looplog.py validate` 合格

## 6. 実装フェーズ

| フェーズ | 内容 | 状態 |
|---|---|---|
| P0 | 立ち上げ: scaffold 導入、本仕様、コマンドデータ正本、ハーネス文書 | 本ループ(loop_000) |
| P1 | core ロジック: 出題キュー(F-02, F-11)、入力照合(F-04, F-05)、落下シミュレーション(F-06, F-09)、スコア(F-08)、セッション管理(F-07)、データ検証テスト(T-001〜T-006) | 完了(loop_002〜004) |
| P2 | UI: レベル選択(F-02, F-12)、ゲーム画面(F-03〜F-06)、結果画面(F-10)、Vercel 公開(N-01) | 完了(loop_006〜007)— https://command-type.vercel.app |
| P3 | 磨き込み: 効果音・演出(F-13)、ミックスモード(F-14) | 未着手 |

## 7. デザイン(視覚仕様)

方向性(2026-08-05 人間指示): **背景はやわらかく明るい配色、フォントサイズは大きめ**。
端末モチーフは書体(等幅フォント)とプロンプト記号で残し、色は紙のように穏やかにする。

実装方式: CSS フレームワークは導入せず、`src/app/globals.css` のデザイントークン
(CSS カスタムプロパティ)に一元化する。コンポーネントはトークンのみを参照し、
色・サイズの直値をコンポーネント内に書かない。フォントは `next/font` で読み込む。

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#faf6ef`(生成り) | 画面全体の背景。やわらかい明るさの基調 |
| `--bg-panel` | `#ffffff` | カード・結果画面のパネル面 |
| `--bg-danger` | `#fdeae7`(淡紅) | 底線接近時の警告帯 |
| `--line` | `#e3dcce` | 罫線・区切り |
| `--text` | `#3a3630`(墨色) | 主文字。真っ黒は使わない |
| `--text-dim` | `#8a8375` | 解説文・補助文字 |
| `--typed` | `#2f6fbf`(青) | 入力済みの一致部分 |
| `--miss` | `#d0453b`(紅) | 打ち間違い・ミス・ライフ減少 |
| `--prompt` | `#2e8b57`(緑) | プロンプト記号・クリア演出 |
| `--accent` | `#c98a1b`(アンバー) | コンボ・スコア・上級バッジ |

書体・サイズ(大きめ基調。rem + clamp で画面幅追従):

| トークン | 値 | 用途 |
|---|---|---|
| `--font-mono` | JetBrains Mono, ui-monospace | コマンド・入力欄 |
| `--font-body` | Zen Kaku Gothic New, sans-serif | 解説文・UI |
| 基準文字サイズ | `18px`(html) | 全体を大きめに |
| `--fs-cmd` | `clamp(2.2rem, 5vw, 3.2rem)` | 落下中のコマンド(画面の主役) |
| `--fs-desc` | `clamp(1rem, 2.4vw, 1.25rem)` | 解説文・カテゴリバッジ |
| `--fs-input` | `1.8rem` | 入力欄 |

状態と色の対応(判断は core、適用は CSS クラスの分担):

- 入力照合(F-04): 一致部分 `--typed` / 最初のミス以降 `--miss` / 未入力 `--text-dim`
- 底線接近(F-06): 残り高度 25% 未満で落下ブロックに `.danger` を付与し警告表示
- カテゴリバッジ: 13 分類は初級=緑・中級=青・上級=アンバーの 3 系統に集約して色分け
- クリア/ミス演出: CSS アニメーションのみで実装(JS タイマーを持ち込まない)
- コントラスト: 主要な文字/背景の組は WCAG AA(4.5:1)以上を保つ

## 8. スコープ外

(やらないことの明記 — GEN-SCOPE の予防線)

- コマンドの**実行**・シェルエミュレーション(あくまでタイピング練習)
- オプション・引数付きの長文コマンド出題(v1 はコマンド名のみ。拡張はスペック改訂から)
- オンラインランキング・ユーザーアカウント・サーバサイド機能
- モバイルでの入力体験の最適化(表示のみ保証)
- 英語 UI(v1 は日本語のみ)
