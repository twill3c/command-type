# TEST_SPEC.md — command-type

<!-- scaffold template v1.6.0 から展開(2026-08-05) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用する(本プロジェクトは Node/TypeScript のため、
  テンプレート既定の pytest を読み替える)。高速ループは `npm run verify:fast`、
  完了条件は `npm run verify`(typecheck + lint + test + build)
- 合否判定に使う品質ゲートコマンドはパイプやページャを通さず素で実行し、exit code で判定する
  (bungo-type HC-004 の教訓)
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## ケース一覧

データ検証(P1 冒頭で実装。正は SPEC §4・§5):

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | commands.json の件数 | 150 件ちょうど |
| T-002 | F-01, §4 | レベル別件数(category → level を引いて集計) | 初級 50 / 中級 50 / 上級 50 |
| T-003 | F-01, §4 | カテゴリ別件数 | SPEC §4 の表と完全一致(13 カテゴリ) |
| T-004 | F-01 | `cmd` の一意性 | 重複 0 件 |
| T-005 | F-01, §5 | `cmd` の文字種 | `[a-z0-9+._-]+` に全件一致 |
| T-006 | F-01, §5 | `desc` の長さ | 全件 8〜40 文字 |
| T-007 | F-01 | `category` の参照整合 | 全件が categories 定義済み id を指す |

core ロジック(P1。関数シグネチャ確定時にケースを具体化する):

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-11 | シード付き出題キュー生成(レベル指定) | 同一シードで同一順序・セッション内重複なし・該当レベルのコマンドのみ |
| T-011 | F-02 | レベル→カテゴリのフィルタ | 初級で上級カテゴリのコマンドが出ない(全レベル総当たり) |
| T-012 | F-04 | 入力照合(前方一致・不一致位置・完全一致) | 境界含め正しい判定(空入力 / 1 字違い / 完全一致) |
| T-013 | F-05 | Enter 確定 | 一致→クリア+次出題 / 不一致→ミス+入力保持+コンボ 0 |
| T-014 | F-06 | 落下シミュレーション(経過時間入力の純関数) | 所定時間で底到達判定・ライフ減・次出題 |
| T-015 | F-09 | 落下速度の漸増 | クリア数 n の速度が算式どおり(レベル別基準値) |
| T-016 | F-08 | スコア算式 | 基礎点+高度ボーナス+コンボの合成が算式どおり(0 コンボ / 連続コンボ) |
| T-017 | F-07 | セッション終了条件 | 20 問完了 or ライフ 0 で終了状態へ遷移 |
| T-018 | F-10 | 結果集計 | 正確率・CPM・ミス一覧が入力イベント列から正しく導出される |
| T-019 | F-12 | ハイスコア保存(storage 抽象を注入) | レベル別に保存・更新は上回ったときのみ |
| T-020 | F-14 | ミックスモードの出題キュー | 母集団 150・重複なし・3 レベルすべてのカテゴリが出現し得る・同一シードで再現 |
| T-021 | F-14 | ミックスモードの落下基準とハイスコア | 落下基準は中級と同値 / ハイスコアは 3 レベルと独立キー |

UI(P2。コンポーネントテストは最小限とし、ロジックは core 側でテストする):

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-03 | 落下表示に cmd / desc / カテゴリ名の 3 点が揃う | 描画要素の存在確認 |
| T-031 | N-03 | 入力欄が半角英数のみ受け付ける | IME 経由の全角入力が拒否される |

効果音(F-13)は Web Audio の実発音を伴うため自動テスト対象外とし、実機スモークの手動確認項目とする(トグルの表示・設定保存は目視)。演出(フラッシュ)も同様。

## カバレッジ基準(SPEC §5)

`src/core/`: lines・functions・statements ≥ 90%、branches ≥ 85%。
ゲートを緩める変更(閾値引き下げ・テスト削除・skip)は人間の承認なしに行わない。
